# 搜题答案助手 v2.6.0 更新说明

## 本次更新

- 重做插件弹窗，将弹窗从配置表单改为轻量操作面板。
- 弹窗只保留配置状态、当前文字/截图模型摘要、快速模式、截图识题、测试连接、设置和教程入口。
- API Key、端点、手动模型 Code、模型全量列表和高级参数全部移入完整设置中心。
- 弹窗主按钮可直接触发当前网页截图识题；未配置时会引导用户进入设置。
- 支持从弹窗向当前页面发送“截图识题”和“划词搜题”指令。

## 兼容性

- 继续兼容旧版本地配置字段。
- 权限未增加，仍为 `activeTab`、`storage`、`tabs`。
