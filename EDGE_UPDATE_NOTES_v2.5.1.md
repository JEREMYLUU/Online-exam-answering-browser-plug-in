# 搜题答案助手 v2.5.1 更新说明

## 本次更新

- 精简插件弹窗，只保留常用配置：服务商、API Key、模型、保存和测试。
- 端点、手动模型 Code、Max Tokens、Temperature 等高级设置移入完整设置中心。
- 弹窗服务商列表改为优先展示常用和免费额度平台，降低新用户配置成本。
- 保留完整设置页入口，仍支持全部模型平台和高级配置。

## 兼容性

- 保留原有配置字段，旧版本用户设置可继续读取。
- 权限未增加，仍为 `activeTab`、`storage`、`tabs`。
